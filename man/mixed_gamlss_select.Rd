% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gamlss.R
\name{mixed_gamlss_select}
\alias{mixed_gamlss_select}
\title{Compare GAMLSS candidates with generalized AIC}
\usage{
mixed_gamlss_select(..., k = 2)
}
\arguments{
\item{...}{GAMLSS fits.}

\item{k}{Penalty per effective degree of freedom; \code{2} gives AIC and \code{log(n)} gives a BIC-like penalty.}
}
\value{
Candidate score table. No model is selected automatically.
}
\description{
Compare GAMLSS candidates with generalized AIC
}
\examples{
# Example 1: no candidates
mixed_gamlss_select()
# Example 2: compare scale structures
\donttest{
  if (requireNamespace("gamlss", quietly = TRUE)) {
      d <- mixed_data("longitudinal")
      a <- mixed_gamlss(height ~ time, d, random = "subject")
      b <- mixed_gamlss(height ~ time, d, sigma.formula = ~treatment, random = "subject")
      mixed_gamlss_select(a, b)
  }
}
# Example 3: stronger complexity penalty
\donttest{
  if (requireNamespace("gamlss", quietly = TRUE)) {
      d <- mixed_data("longitudinal")
      a <- mixed_gamlss(height ~ time, d, random = "subject")
      mixed_gamlss_select(a, k = log(nrow(d)))
  }
}
}
