% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nonlinear.R
\name{mixed_nonlinear}
\alias{mixed_nonlinear}
\title{Fit a nonlinear mixed model through an established engine}
\usage{
mixed_nonlinear(
  model,
  data,
  engine = c("nlme", "lme4", "brms"),
  fixed = NULL,
  random = NULL,
  groups = NULL,
  start = NULL,
  ...
)
}
\arguments{
\item{model}{Nonlinear model formula accepted by the selected engine.}

\item{data}{Data frame.}

\item{engine}{\code{nlme}, \code{lme4}, or \code{brms}.}

\item{fixed}{Fixed-effects specification for \code{nlme}.}

\item{random}{Random-effects specification for \code{nlme}.}

\item{groups}{Grouping specification for \code{nlme} when needed.}

\item{start}{Starting values.}

\item{...}{Additional backend arguments.}
}
\value{
A \code{mixedflow_fit}.
}
\description{
Fit a nonlinear mixed model through an established engine
}
\examples{
g<-mixed_data("growth")
# Example 1: nlme logistic growth
\donttest{
  if (requireNamespace("nlme", quietly = TRUE)) {
      st <- stats::getInitial(biomass ~ SSlogis(time, Asym, xmid, scal), g)
      mixed_nonlinear(biomass ~ SSlogis(time, Asym, xmid, scal), g, "nlme", fixed = Asym + 
          xmid + scal ~ 1, random = Asym ~ 1, groups = ~plant, start = st)
  }
}
# Example 2: lme4 nlmer logistic growth
\donttest{
  if (requireNamespace("lme4", quietly = TRUE)) {
      st <- stats::getInitial(biomass ~ SSlogis(time, Asym, xmid, scal), g)
      mixed_nonlinear(biomass ~ SSlogis(time, Asym, xmid, scal) ~ (Asym | plant), 
          g, "lme4", start = st)
  }
}
# Example 3: Bayesian nonlinear formula supplied by the user
\dontrun{
  mixed_nonlinear(brms::bf(biomass ~ Asym/(1 + exp((xmid - time)/scal)), Asym ~ 
      1 + (1 | plant), xmid ~ 1, scal ~ 1, nl = TRUE), g, "brms")
}
}
