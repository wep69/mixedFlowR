% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gamlss.R
\name{mixed_gamlss}
\alias{mixed_gamlss}
\title{Fit a mixed GAMLSS model}
\usage{
mixed_gamlss(
  formula,
  data,
  family = "NO",
  sigma.formula = ~1,
  nu.formula = ~1,
  tau.formula = ~1,
  random = NULL,
  re_random = NULL,
  correlation = NULL,
  ...
)
}
\arguments{
\item{formula}{Location (\code{mu}) formula.}

\item{data}{Data frame.}

\item{family}{GAMLSS family.}

\item{sigma.formula, nu.formula, tau.formula}{Distributional parameter formulas.}

\item{random}{Optional grouping-factor name added through \code{gamlss::random}.}

\item{re_random}{Optional \code{nlme}-style random formula added through \code{gamlss::re}.}

\item{correlation}{Optional \code{nlme} correlation structure used with \code{re_random}.}

\item{...}{Additional \code{gamlss::gamlss} arguments.}
}
\value{
A \code{mixedflow_fit}.
}
\description{
Fit a mixed GAMLSS model
}
\examples{
d<-mixed_data("longitudinal")
# Example 1: Gaussian GAMLSS with subject random effect
\donttest{
  if (requireNamespace("gamlss", quietly = TRUE)) mixed_gamlss(height ~ treatment * 
      time, d, family = "NO", random = "subject")
}
# Example 2: model scale by treatment
\donttest{
  if (requireNamespace("gamlss", quietly = TRUE)) mixed_gamlss(height ~ time, 
      d, family = "NO", sigma.formula = ~treatment, random = "subject")
}
# Example 3: random effect represented through nlme
\donttest{
  if (requireNamespace("gamlss", quietly = TRUE) && requireNamespace("nlme", 
      quietly = TRUE)) mixed_gamlss(height ~ time, d, family = "NO", re_random = ~1 | 
      subject)
}
}
