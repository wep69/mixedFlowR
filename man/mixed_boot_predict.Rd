% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{mixed_boot_predict}
\alias{mixed_boot_predict}
\title{Bootstrap predictions from a linear mixed model}
\usage{
mixed_boot_predict(
  object,
  newdata,
  B = 999,
  level = 0.95,
  re.form = NA,
  seed = 123,
  ...
)
}
\arguments{
\item{object}{Fitted \code{lmer} model.}

\item{newdata}{Prediction data.}

\item{B}{Number of bootstrap replicates.}

\item{level}{Confidence level.}

\item{re.form}{Random-effects prediction specification passed to \code{predict}.}

\item{seed}{Seed.}

\item{...}{Additional \code{bootMer} arguments.}
}
\value{
Prediction table with percentile intervals.
}
\description{
Bootstrap predictions from a linear mixed model
}
\examples{
d<-mixed_data("longitudinal")
# Example 1: population predictions
\donttest{
  if (requireNamespace("lme4", quietly = TRUE)) {
      m <- mixed_fit(height ~ time + (1 | subject), d)
      g <- data.frame(time = c(0, 5), subject = d$subject[1])
      mixed_boot_predict(m, g, B = 20)
  }
}
# Example 2: conditional predictions
\donttest{
  if (requireNamespace("lme4", quietly = TRUE)) {
      m <- mixed_fit(height ~ time + (1 | subject), d)
      g <- d[1:3, ]
      mixed_boot_predict(m, g, B = 20, re.form = NULL)
  }
}
# Example 3: 90 percent interval
\donttest{
  if (requireNamespace("lme4", quietly = TRUE)) {
      m <- mixed_fit(height ~ treatment * time + (1 | subject), d)
      mixed_boot_predict(m, d[1:4, ], B = 20, level = 0.9)
  }
}
}
