% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayes.R
\name{mixed_loo}
\alias{mixed_loo}
\title{Leave-one-out predictive assessment for Bayesian models}
\usage{
mixed_loo(object, ...)
}
\arguments{
\item{object}{One or more Bayesian fits.}

\item{...}{Additional fits or arguments accepted by \code{brms::loo} when a single model is supplied.}
}
\value{
A LOO object or a model-comparison table.
}
\description{
Leave-one-out predictive assessment for Bayesian models
}
\examples{
# Example 1: single model
\dontrun{
  d <- mixed_data("longitudinal")
  m <- mixed_bayes(height ~ time + (1 | subject), d, chains = 2, iter = 1000)
  mixed_loo(m)
}
# Example 2: treatment model
\dontrun{
  d <- mixed_data("longitudinal")
  m <- mixed_bayes(height ~ treatment * time + (1 | subject), d, chains = 2, 
      iter = 1000)
  mixed_loo(m)
}
# Example 3: compare two models
\dontrun{
  d <- mixed_data("longitudinal")
  a <- mixed_bayes(height ~ time + (1 | subject), d, chains = 2, iter = 1000)
  b <- mixed_bayes(height ~ treatment * time + (1 | subject), d, chains = 2, 
      iter = 1000)
  mixed_loo(a, b)
}
}
