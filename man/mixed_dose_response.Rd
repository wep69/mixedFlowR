% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nonlinear.R
\name{mixed_dose_response}
\alias{mixed_dose_response}
\title{Fit a four-parameter dose-response mixed model}
\usage{
mixed_dose_response(data, response, dose, group, start = NULL, ...)
}
\arguments{
\item{data}{Data frame.}

\item{response, dose}{Response and quantitative dose columns.}

\item{group}{Experimental-unit grouping column.}

\item{start}{Optional pooled starting values.}

\item{...}{Additional \code{nlme} arguments.}
}
\value{
A nonlinear \code{mixedflow_fit}.
}
\description{
Fit a four-parameter dose-response mixed model
}
\examples{
d<-mixed_data("dose_response")
# Example 1: four-parameter logistic
\donttest{
  if (requireNamespace("nlme", quietly = TRUE)) mixed_dose_response(d, "response", 
      "dose", "unit")
}
# Example 2: subset of units
\donttest{
  if (requireNamespace("nlme", quietly = TRUE)) mixed_dose_response(subset(d, 
      as.integer(sub("U", "", unit)) <= 20), "response", "dose", "unit")
}
# Example 3: user-supplied pooled starts
\donttest{
  if (requireNamespace("nlme", quietly = TRUE)) {
      st <- stats::getInitial(response ~ SSfpl(dose, A, B, xmid, scal), d)
      mixed_dose_response(d, "response", "dose", "unit", st)
  }
}
}
