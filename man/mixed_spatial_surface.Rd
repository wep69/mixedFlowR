% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agronomy.R
\name{mixed_spatial_surface}
\alias{mixed_spatial_surface}
\title{Extract and plot the fitted SpATS spatial surface}
\usage{
mixed_spatial_surface(object, grid = c(50, 50))
}
\arguments{
\item{object}{\code{SpATS} or wrapped field-trial fit.}

\item{grid}{Number of grid points in x and y directions.}
}
\value{
A list containing grid data and a publication-ready heat map.
}
\description{
Extract and plot the fitted SpATS spatial surface
}
\examples{
s<-mixed_data("spatial")
# Example 1: default surface
\donttest{
  if (requireNamespace("SpATS", quietly = TRUE)) {
      m <- mixed_spatial_field(s, "yield", "genotype", "col", "row", random = ~block)
      mixed_spatial_surface(m)
  }
}
# Example 2: compact grid
\donttest{
  if (requireNamespace("SpATS", quietly = TRUE)) {
      m <- mixed_spatial_field(s, "yield", "genotype", "col", "row", random = ~block)
      mixed_spatial_surface(m, c(20, 20))
  }
}
# Example 3: genotype random
\donttest{
  if (requireNamespace("SpATS", quietly = TRUE)) {
      m <- mixed_spatial_field(s, "yield", "genotype", "col", "row", random = ~block, 
          genotype_as_random = TRUE)
      mixed_spatial_surface(m, c(25, 25))
  }
}
}
