% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-fit.R
\name{mixed_blup}
\alias{mixed_blup}
\title{Extract conditional random-effect predictions (BLUPs/modes)}
\usage{
mixed_blup(object, group = NULL)
}
\arguments{
\item{object}{Fitted object or \code{mixedflow_fit}.}

\item{group}{Optional grouping component.}
}
\value{
Backend-specific random-effect table/list.
}
\description{
Extract conditional random-effect predictions (BLUPs/modes)
}
\examples{
z<-mixed_data("longitudinal")
# Example 1: random intercept and slope BLUPs
\donttest{
  if (requireNamespace("lme4", quietly = TRUE)) {
      f <- mixed_fit(height ~ treatment * time + (time | subject), z)
      mixed_blup(f)
  }
}
# Example 2: selected grouping factor
\donttest{
  if (requireNamespace("lme4", quietly = TRUE)) {
      f <- mixed_fit(height ~ time + (1 | subject), z)
      mixed_blup(f, "subject")
  }
}
# Example 3: glmmTMB conditional effects
c <- mixed_data("counts")
\donttest{
  if (requireNamespace("glmmTMB", quietly = TRUE)) {
      f <- mixed_glmm(count ~ treatment + (1 | plot), c, family = poisson(), 
          engine = "glmmTMB")
      mixed_blup(f)
  }
}
}
