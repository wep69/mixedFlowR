% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legacy.R
\name{compare_legacy_mixed}
\alias{compare_legacy_mixed}
\title{Compare legacy split-plot and mixed-model representations}
\usage{
compare_legacy_mixed(data, response, block, whole, sub, REML = TRUE)
}
\arguments{
\item{data}{Data frame.}

\item{response, block, whole, sub}{Column names.}

\item{REML}{Use REML for the mixed model.}
}
\value{
A list containing legacy and \code{lme4} fits plus fixed and variance-component summaries.
}
\description{
Fits the classical error-stratum ANOVA and the corresponding Gaussian mixed model. This is intended for balanced designs and teaching/auditing.
}
\examples{
d <- mixed_data("splitplot")
# Example 1: canonical comparison
\donttest{
  if (requireNamespace("lme4", quietly = TRUE)) compare_legacy_mixed(d, "yield", 
      "block", "variety", "nitrogen")
}
# Example 2: nitrogen factor
d$nf <- factor(d$nitrogen)
\donttest{
  if (requireNamespace("lme4", quietly = TRUE)) compare_legacy_mixed(d, "yield", 
      "block", "variety", "nf")
}
# Example 3: ML sensitivity fit
\donttest{
  if (requireNamespace("lme4", quietly = TRUE)) compare_legacy_mixed(d, "yield", 
      "block", "variety", "nitrogen", REML = FALSE)
}
}
