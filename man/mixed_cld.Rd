% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{mixed_cld}
\alias{mixed_cld}
\title{Compact-letter display for estimated marginal means}
\usage{
mixed_cld(object, specs = NULL, adjust = "tukey", alpha = 0.05, ...)
}
\arguments{
\item{object}{Fitted model or \code{emmGrid}.}

\item{specs}{EMM specification when needed.}

\item{adjust}{Multiplicity adjustment.}

\item{alpha}{Familywise threshold.}

\item{...}{Additional \code{emmeans::cld} arguments.}
}
\value{
A compact-letter display. Letters encode non-rejection and should not be interpreted as proof of equality.
}
\description{
Compact-letter display for estimated marginal means
}
\examples{
d<-mixed_data("splitplot")
# Example 1: variety grouping
\donttest{
  if (requireNamespace("lme4", quietly = TRUE) && requireNamespace("emmeans", 
      quietly = TRUE) && requireNamespace("multcompView", quietly = TRUE)) {
      m <- mixed_fit(yield ~ variety + nitrogen + (1 | block), d)
      mixed_cld(m, ~variety)
  }
}
# Example 2: Sidak adjustment
\donttest{
  if (requireNamespace("lme4", quietly = TRUE) && requireNamespace("emmeans", 
      quietly = TRUE) && requireNamespace("multcompView", quietly = TRUE)) {
      m <- mixed_fit(yield ~ variety + nitrogen + (1 | block), d)
      mixed_cld(m, ~variety, "sidak")
  }
}
# Example 3: existing EMM grid
\donttest{
  if (requireNamespace("lme4", quietly = TRUE) && requireNamespace("emmeans", 
      quietly = TRUE) && requireNamespace("multcompView", quietly = TRUE)) {
      m <- mixed_fit(yield ~ variety + nitrogen + (1 | block), d)
      e <- mixed_emmeans(m, ~variety)
      mixed_cld(e)
  }
}
}
