% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agronomy.R
\name{mixed_spatial_field}
\alias{mixed_spatial_field}
\title{Fit a P-spline spatial field-trial model}
\usage{
mixed_spatial_field(
  data,
  response,
  genotype,
  col,
  row,
  fixed = NULL,
  random = NULL,
  genotype_as_random = FALSE,
  nseg = c(6, 6),
  ...
)
}
\arguments{
\item{data}{Field-trial data.}

\item{response}{Response column.}

\item{genotype}{Genotype/treatment column.}

\item{col, row}{Numeric field coordinates.}

\item{fixed}{Optional fixed design term: a one-sided formula (\code{~ block}) or the name(s) of design columns.}

\item{random}{Optional random design term: a one-sided formula (\code{~ block}) or the name(s) of design columns.}

\item{genotype_as_random}{Treat genotype as random.}

\item{nseg}{Number of P-spline segments in each direction.}

\item{...}{Additional \code{SpATS::SpATS} arguments.}
}
\value{
A \code{mixedflow_fit}.
}
\description{
Fit a P-spline spatial field-trial model
}
\examples{
s<-mixed_data("spatial")
# Example 1: genotype fixed, blocks random
\donttest{
  if (requireNamespace("SpATS", quietly = TRUE)) mixed_spatial_field(s, "yield", 
      "genotype", "col", "row", random = ~block)
}
# Example 2: genotype random
\donttest{
  if (requireNamespace("SpATS", quietly = TRUE)) mixed_spatial_field(s, "yield", 
      "genotype", "col", "row", random = ~block, genotype_as_random = TRUE)
}
# Example 3: finer spatial basis
\donttest{
  if (requireNamespace("SpATS", quietly = TRUE)) mixed_spatial_field(s, "yield", 
      "genotype", "col", "row", random = ~block, nseg = c(8, 10))
}
}
