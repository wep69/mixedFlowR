% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gamlss.R
\name{mixed_gamlss_parameters}
\alias{mixed_gamlss_parameters}
\title{Extract fitted GAMLSS distribution parameters}
\usage{
mixed_gamlss_parameters(object, parameters = c("mu", "sigma", "nu", "tau"))
}
\arguments{
\item{object}{Fitted GAMLSS object.}

\item{parameters}{Parameters to attempt: \code{mu}, \code{sigma}, \code{nu}, \code{tau}.}
}
\value{
A data frame of fitted distributional parameters.
}
\description{
Extract fitted GAMLSS distribution parameters
}
\examples{
d<-mixed_data("longitudinal")
# Example 1: mu and sigma
\donttest{
  if (requireNamespace("gamlss", quietly = TRUE)) {
      m <- mixed_gamlss(height ~ time, d, random = "subject")
      mixed_gamlss_parameters(m)
  }
}
# Example 2: mu only
\donttest{
  if (requireNamespace("gamlss", quietly = TRUE)) {
      m <- mixed_gamlss(height ~ time, d, random = "subject")
      mixed_gamlss_parameters(m, "mu")
  }
}
# Example 3: heteroscedastic scale model
\donttest{
  if (requireNamespace("gamlss", quietly = TRUE)) {
      m <- mixed_gamlss(height ~ time, d, sigma.formula = ~treatment, random = "subject")
      mixed_gamlss_parameters(m)
  }
}
}
