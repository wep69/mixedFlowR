% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayes.R
\name{mixed_prior}
\alias{mixed_prior}
\title{Inspect or design priors for a Bayesian mixed model}
\usage{
mixed_prior(formula, data = NULL, family = stats::gaussian(), ...)
}
\arguments{
\item{formula}{Model formula or a fitted \code{brmsfit}/\code{mixedflow_fit}.}

\item{data}{Data frame when a formula is supplied.}

\item{family}{Family when a formula is supplied.}

\item{...}{Additional \code{brms::get_prior} arguments.}
}
\value{
Prior table.
}
\description{
Inspect or design priors for a Bayesian mixed model
}
\examples{
d<-mixed_data("longitudinal")
# Example 1: available priors
\donttest{if(requireNamespace("brms",quietly=TRUE)) mixed_prior(height~time+(1|subject),d)}
# Example 2: random-slope priors
\donttest{if(requireNamespace("brms",quietly=TRUE)) mixed_prior(height~time+(time|subject),d)}
# Example 3: binomial prior classes
\donttest{
  if (requireNamespace("brms", quietly = TRUE)) {
      b <- mixed_data("binomial")
      mixed_prior(diseased | trials(total) ~ treatment + (1 | block), b, binomial())
  }
}
}
