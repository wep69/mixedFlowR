% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/communication.R
\name{mixed_plot}
\alias{mixed_plot}
\title{Create publication-ready mixed-model graphics}
\usage{
mixed_plot(
  object,
  type = c("raw", "residuals", "fixed", "random", "curve", "covariance", "bootstrap",
    "spatial"),
  x = NULL,
  y = NULL,
  by = NULL,
  values = NULL,
  show_data = TRUE,
  file = NULL,
  width = 7,
  height = 5,
  dpi = 600,
  ...
)
}
\arguments{
\item{object}{Fitted model or mixedFlowR result.}

\item{type}{Plot type: \code{raw}, \code{residuals}, \code{fixed}, \code{random}, \code{curve}, \code{covariance}, \code{bootstrap}, or \code{spatial}.}

\item{x}{Optional quantitative x-variable for raw or curve plots.}

\item{y}{Optional response variable for raw plots. If omitted, the fitted response is inferred when possible.}

\item{by}{Optional grouping factor for raw or curve plots.}

\item{values}{Prediction values for curves.}

\item{show_data}{For curve plots, overlay the observed response whenever stored model data and the response can be identified.}

\item{file}{Optional output file. Use \code{.pdf} or \code{.svg} for vector output and \code{.tiff}/\code{.png} for raster output.}

\item{width, height}{Output dimensions in inches.}

\item{dpi}{Raster resolution; the default is 600 dpi.}

\item{...}{Additional arguments passed to underlying helpers.}
}
\value{
A \code{ggplot} object, after optional export.
}
\description{
Create publication-ready mixed-model graphics
}
\examples{
d<-mixed_data("longitudinal")
# Example 1: observed measurements
\donttest{
  if (requireNamespace("lme4", quietly = TRUE)) {
      m <- mixed_fit(height ~ time + (1 | subject), d)
      mixed_plot(m, "raw", x = "time", by = "treatment")
  }
}
# Example 2: residual diagnostic
\donttest{
  if (requireNamespace("lme4", quietly = TRUE)) {
      m <- mixed_fit(height ~ time + (1 | subject), d)
      mixed_plot(m, "residuals")
  }
}
# Example 3: treatment curves with observed data and model uncertainty
\donttest{
  if (requireNamespace("lme4", quietly = TRUE) && requireNamespace("emmeans", 
      quietly = TRUE)) {
      m <- mixed_fit(height ~ treatment * time + (1 | subject), d)
      mixed_plot(m, "curve", x = "time", by = "treatment", values = seq(0, 5, 
          0.25))
  }
}
}
