% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{mixed_boot}
\alias{mixed_boot}
\title{Bootstrap inference for mixed models}
\usage{
mixed_boot(
  object,
  statistic = c("fixed", "variance"),
  B = 999,
  method = c("parametric", "wild"),
  interval = c("percentile", "BCa"),
  level = 0.95,
  cluster = NULL,
  seed = 123,
  ...
)
}
\arguments{
\item{object}{Fitted \code{lmer}/\code{rlmer} or wrapped fit.}

\item{statistic}{\code{fixed} or \code{variance}.}

\item{B}{Number of bootstrap replicates.}

\item{method}{\code{parametric} or \code{wild}.}

\item{interval}{\code{percentile} or \code{BCa}.}

\item{level}{Confidence level.}

\item{cluster}{Cluster variable name or vector for methods that require it.}

\item{seed}{Reproducible seed.}

\item{...}{Additional backend arguments.}
}
\value{
A \code{mixedflow_boot} object containing native output and interval estimates.
}
\description{
Bootstrap inference for mixed models
}
\examples{
d<-mixed_data("longitudinal")
# Example 1: fixed-effect parametric bootstrap
\donttest{
  if (requireNamespace("lme4", quietly = TRUE)) {
      m <- mixed_fit(height ~ time + (1 | subject), d)
      mixed_boot(m, "fixed", B = 20, seed = 1)
  }
}
# Example 2: variance-component bootstrap
\donttest{
  if (requireNamespace("lme4", quietly = TRUE)) {
      m <- mixed_fit(height ~ time + (1 | subject), d)
      mixed_boot(m, "variance", B = 20, seed = 2)
  }
}
# Example 3: BCa via confintROB
\dontrun{
  if (requireNamespace("lme4", quietly = TRUE) && requireNamespace("confintROB", 
      quietly = TRUE)) {
      m <- mixed_fit(height ~ time + (1 | subject), d)
      mixed_boot(m, "fixed", B = 999, interval = "BCa", cluster = "subject")
  }
}
}
