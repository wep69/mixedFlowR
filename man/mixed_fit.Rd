% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-fit.R
\name{mixed_fit}
\alias{mixed_fit}
\title{Fit a mixed model through an appropriate engine}
\usage{
mixed_fit(
  formula,
  data,
  family = stats::gaussian(),
  engine = c("auto", "lme4", "nlme", "glmmTMB"),
  random = NULL,
  correlation = NULL,
  weights = NULL,
  REML = TRUE,
  ziformula = ~0,
  dispformula = ~1,
  ...
)
}
\arguments{
\item{formula}{Model formula. For \code{lme4}/\code{glmmTMB}, random effects may use \code{|} terms.}

\item{data}{Data frame.}

\item{family}{A base-R family object or family name.}

\item{engine}{One of \code{auto}, \code{lme4}, \code{nlme}, or \code{glmmTMB}.}

\item{random}{\code{nlme} random-effects formula/list when applicable.}

\item{correlation}{Optional \code{nlme} correlation structure.}

\item{weights}{Optional \code{nlme} variance function/weights.}

\item{REML}{Logical; use REML for Gaussian \code{lme4}/\code{nlme} fits.}

\item{ziformula, dispformula}{\code{glmmTMB} zero-inflation and dispersion formulas.}

\item{...}{Additional backend arguments.}
}
\value{
A \code{mixedflow_fit} wrapper retaining the native fitted model.
}
\description{
Provides a common entry point for \code{lme4}, \code{nlme} and \code{glmmTMB} while retaining the original backend object.
Automatic routing is conservative: Gaussian models with residual correlation/variance structures go to \code{nlme};
otherwise Gaussian and standard GLMM formulas go to \code{lme4} unless an engine is requested explicitly.
}
\examples{
d <- mixed_data("splitplot")
# Example 1: Gaussian split-plot LMM
\donttest{
  if (requireNamespace("lme4", quietly = TRUE)) mixed_fit(yield ~ variety * nitrogen + 
      (1 | block) + (1 | block:variety), d)
}
# Example 2: longitudinal LMM with random slope
z <- mixed_data("longitudinal")
\donttest{
  if (requireNamespace("lme4", quietly = TRUE)) mixed_fit(height ~ treatment * 
      time + (time | subject), z)
}
# Example 3: correlated residuals via nlme
\donttest{
  if (requireNamespace("nlme", quietly = TRUE))
    mixed_fit(height ~ treatment * time, z, engine = "nlme",
              random = ~time | subject,
              correlation = nlme::corAR1(form = ~time | subject))
}
}
